# copier en .env et remplir. .env est ignoré par git.

# MySQL
DB_HOST=localhost
DB_USER=
DB_PASS=
DB_NAME=kajbak

# deux chaines aleatoires differentes, a generer avec :
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
JWT_SECRET=
